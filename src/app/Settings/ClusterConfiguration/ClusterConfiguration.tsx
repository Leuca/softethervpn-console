import * as React from 'react';
import { UnderConstruction } from '@app/UnderConstruction';

const ClusterConfig: React.FunctionComponent = () => <UnderConstruction pageName="Clustering Configuration" />;

export { ClusterConfig };
